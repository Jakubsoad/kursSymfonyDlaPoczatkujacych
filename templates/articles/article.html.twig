{% extends 'base.html.twig' %}

{% block title %}
    {{ article.title }}
{% endblock %}

{% block body %}
    <div>
        <h1>{{ article.title }}</h1>
        <span>{{ article.content }}</span>
    </div>
{% endblock %}
